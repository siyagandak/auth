server:
  port: 840
spring:
  jpa:
    hibernate:
      ddl-auto: update
      database-platform: org.hibernate.dialect.PostgreSQLDialect

  datasource:
    url: jdbc:postgresql://localhost:5432/auth
    username: postgres
    password: rOOt

app:
  jwtSecret: bezKoderSecretKey
  jwtExpirationMs: 86400000
  smsUser : fbc_chamunorwa
  smsPassword : LKDrefLq
